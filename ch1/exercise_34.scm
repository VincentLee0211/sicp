;; 假定我们定义了

(define f (lambda (g) (g 2)))

;; 而后有:

(f square)
;; 4

(f (lambda (z) (* z (+ z 1))))
;; 6

;; 如果我们(坚持)要求解释器去求值(f f), 那会发生什么情况呢?

;; -------------------------------------------------------------------------------------------------

;; (f f)
;; ((lambda (g) (g 2)) (lambda (g) (g 2)))
;; ((lambda (g) (g 2)) 2)
;; (2 2)
;; 因为第一个`2`不是正确的运算符, 所以解释器报错
